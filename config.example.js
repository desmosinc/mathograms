//This is for use in local development
//To enable email, get a key from mandrill (mandrill.com)
//put it in here, and rename this to config.js (which is .gitignore'd)

var config = {}

//get a key to mandrill (www.mandrill.com) and add it here
config.mandrillKey = '[YOUR KEY]'

module.exports = config;